_ = require('lodash')
expect = require('expect.js')
proxy = require("../nlopt.js")
request = require("request")

describe('basic', ()->
  before(()->
    
  )
  after(()->
    
  )
  beforeEach(()->
  )
  it('example', ()->

  )
)